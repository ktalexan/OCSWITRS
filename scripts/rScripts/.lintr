linters: linters_with_defaults(
    indentation_linter(indent = 4L, hanging_indent_style = c("tidy", "always", "never"), assignment_as_infix = TRUE),
    line_length_linter(length = NULL),
    object_usage_linter = object_usage_linter(),
    object_name_linter(styles = "camelCase"),
    spaces_left_parentheses_linter = spaces_left_parentheses_linter(),
    spaces_around_operators_linter = spaces_around_operators_linter(),
    assignment_linter = assignment_linter()
    # commented_code_linter is disabled to allow commenting code sections
)
